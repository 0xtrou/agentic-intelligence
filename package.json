{
  "name": "agentic-intelligence",
  "version": "0.0.0",
  "private": true,
  "description": "Autonomous trading intelligence for Bybit crypto markets",
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "test": "turbo run test",
    "test:coverage": "turbo run test -- --coverage",
    "lint": "turbo run lint",
    "clean": "turbo run clean && rm -rf node_modules"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^2.1.9",
    "turbo": "^2.8.15",
    "typescript": "^5.9.3"
  },
  "packageManager": "pnpm@9.0.0",
  "engines": {
    "node": ">=22.0.0",
    "pnpm": ">=9.0.0"
  }
}
